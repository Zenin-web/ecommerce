import { baseApi } from "../baseApi/baseApi";
// import { API_TAGS } from "@/constants/apiTags";
// import { FAVORITE_PATH } from "./path";

// TODO (talaba uchun): getMyFavorites, toggleFavorite
export const favoriteApi = baseApi.injectEndpoints({
  endpoints: () => ({}),
});

